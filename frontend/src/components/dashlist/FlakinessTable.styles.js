import styled from 'styled-components';

export const TableContainer = styled.div`
    border-top: 1px solid var(--tonic-grey);

    button {
        margin: var(--space-16) var(--space-8) var(--space-16) 0;
    }
`;
